{
  "name": "jago-root",
  "version": "1.0.0",
  "private": true,
  "description": "Repo root proxy - real app lives in app/. This delegates npm scripts so DigitalOcean App Platform (with source_dir: /) still works even if .do/app.yaml source_dir is not applied.",
  "scripts": {
    "install:app": "cd app && npm ci --include=dev",
    "build": "cd app && npm ci --include=dev && npm run build",
    "check": "cd app && npm run check",
    "dev": "cd app && npm run dev",
    "start": "cd app && node dist/index.js",
    "postinstall": "cd app && npm ci --include=dev"
  },
  "engines": {
    "node": "20.x"
  }
}
